import 'package:flutter/material.dart';

class CinemaTab extends StatelessWidget {
  const CinemaTab({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
          child: Text('cinema Tab'),
        ));
  }
}
